# === Environment Configuration Example ===
# NU COMITE acest fisier cu valori reale. Copiaza-l in .env si pune valorile tale locale.

# Email credentials pentru nodemailer (Gmail app password recomandat)
EMAIL_USER=example@gmail.com
EMAIL_PASS=app-password-16chars

# Feature flags
GPT5_ENABLED=false

# Supabase (mută aceste valori din cod pentru securitate)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key

# NOTE:
# 1. Creeaza un .env local: cp .env.example .env
# 2. NU publica niciodata parole reale in repo.
# 3. Schimba imediat parolele daca au fost expuse.
